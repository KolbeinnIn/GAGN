create database 0908002640_SaleCo;
use 0908002640_SaleCo;

create table Customer(
	CUS_CODE int not null primary key,
    CUS_FNAME VARCHAR(18) NOT NULL,
    CUS_LNAME VARCHAR(18) NOT NULL,
    CUS_INITIAL CHAR(
    CUS_AREACODE
    CUS_PHONE
    CUS_BALANCE
);